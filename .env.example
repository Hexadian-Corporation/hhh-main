# Shared JWT secret — used by all H³ backend services and the auth service.
# All services MUST use the same secret for token validation to work.
# Use a random string of at least 32 characters (e.g. openssl rand -base64 32).
HEXADIAN_AUTH_JWT_SECRET=your-secure-secret-here
